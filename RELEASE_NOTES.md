# Monica Steam 1.0.306 第四个公开测试版本补丁

这是第四个公开测试版本的补丁更新，仍处于测试阶段，并非正式版或稳定版。

> [!WARNING]
> 升级前建议备份现有 `maFile`、MDBX 数据及其他重要账号资料。部分功能依赖 Steam
> 服务、账号权限和所在地区，实际可用性可能受 Valve 接口与风控策略影响。

## 应用更新

- 设置的“应用与支持”页面新增“检查更新”，可对比 Monica Steam GitHub Releases 中的最新版本。
- 发现新版本后可按当前设备架构选择对应 APK，显示下载进度，并调用系统安装器完成升级。
- 安装前会校验 APK 包名与当前应用签名；无法匹配设备架构时改为打开 GitHub Release 页面。

## Steam 网络解析

- 网络优化按职责明确区分为“动态网络优化”和“静态网络优化（Hosts）”：传统 DNS 与 HTTPS DoH 统一归入动态解析，固定域名到 IP 的映射继续由静态 Hosts 负责。
- 新增内置优选 Hosts 预设，覆盖 18 个常用 Steam / CDN hostname，可从静态 Hosts 页面右上角菜单手动应用；新安装不会自动写入或启用固定 IP，已有用户自定义 Hosts 也不会在升级时被覆盖。
- 内置优选 Hosts 与手工填写 Hosts 使用同一套 `SteamCustomHostsDns` 静态解析链路；静态命中后直接使用固定 IP，不再先调用动态 DNS / DoH。
- 静态 Hosts 可继续保留 Android System DNS fallback，在固定 CDN 节点未来失效时提供兼容回退。
- 动态网络优化调整 System DNS 的角色：当存在已启用的传统 DNS / DoH 时，System DNS 不再与它们抢第一个响应，而只在动态来源失败时作为 fallback；仅启用 System DNS 时仍直接使用系统解析。
- 修复“开启 DoH 但本地 System DNS 因响应更快而长期抢先，导致 DoH 实际很少被使用”的问题。
- 动态 DNS / DoH 不再把“返回了公网 IP”直接视为成功。缓存 miss 时会收集多个候选，并使用原始 Steam hostname 对候选执行轻量 HTTPS / SNI / 证书可用性验证。
- 最快返回的自定义 DNS / DoH 如果给出不可用路线，会继续尝试其他动态来源；所有动态候选都不可用时，可回退 Android System DNS，避免开启自定义解析后反而导致 Steam 整体不可用。
- 通过验证的动态结果继续使用短期缓存，并合并同 hostname 的并发解析任务，避免每个 HTTP 请求重复进行 HTTPS 探测。
- 普通 DNS 与加密 DoH 保留统一的“解析服务器”管理，可在同一列表启用、关闭、测速和删除。
- 添加解析服务器时会根据地址自动识别普通 DNS 与 HTTPS DoH，并分别显示各自的数量和上限。
- 自定义 DoH 支持填写多个 Bootstrap IPv4 或 IPv6，使应用无需先依赖系统 DNS 查找 DoH 主机；HTTPS 仍使用原始域名完成 SNI 与证书校验。
- 再次填写已有 DoH 地址可以更新或清空 Bootstrap IP，无需先删除解析服务器；修改 DoH endpoint 或 Bootstrap 后会重置旧 DnsOverHttps 实例及其连接池，避免旧 TLS keep-alive 继续复用原地址。
- 修改静态 Hosts、动态解析来源、Bootstrap 或强制刷新缓存后会立即清理空闲的旧 HTTPS 连接，减少“刚保存新配置却仍复用旧 IP”的窗口；不会因此取消正在执行的 HTTPS 请求。
- 强化 IPv4、IPv6、DoH 地址及 Bootstrap 配置校验，忽略损坏和已失效的旧配置条目，并保留现有 DNS、DoH 与启用状态设置。
- 本轮网络优化没有关闭 TLS 证书校验、修改 Steam SNI、安装自定义 CA、建立系统 VPN 或修改 Android 系统 Hosts；网络优化仍限定在 Monica Steam 应用范围内。

## 小窗与系统窗口

- 修复 MIUI 小窗模式下顶栏进入窗口拖拽区、按钮位置过高的问题。
- 修复聊天输入框及 M3E、液态玻璃、固定底栏被小窗底部手势区或三键导航遮挡的问题。
- 修复聚焦搜索框弹出输入法时，M3E、液态玻璃和固定底栏被一起顶到键盘上方的问题。
- 小窗移动期间会保留最近一次有效的系统安全边距，修复固定底栏和页面内容突然跳动的问题。

## MDBX2 数据库

- 引入与 Monica Pass 同源的 Rust MDBX2 数据库引擎，同时保留 MDBX1 兼容能力；现有数据库可继续打开，
  新数据库可选择 MDBX2。
- Steam `maFile` 账号读取、令牌页和扫码页统一使用双引擎仓库，MDBX1 与 MDBX2 中的 Steam 账号均可正常使用。
- 新增 MDBX1 到 MDBX2 的安全迁移流程，迁移前生成计划，迁移后验证项目、条目及对象一致性，异常时保留原库。
- 本地目录、系统文件目录、WebDAV 和 OneDrive 均支持创建、打开与同步 MDBX2 数据库。
- 远程同步新增 Sidecar 状态、断点续传和条件写入；WebDAV 使用 `If-None-Match` 与 `If-Match`
  防止覆盖远端新版本，OneDrive 与 WebDAV 通过文件流处理数据库，降低大文件同步时的内存占用。
- 新增数据库快照、提交历史、诊断、健康检查与修复界面，可查看同步及迁移状态并处理可恢复问题。
- MDBX2 原生模块包含 `arm64-v8a`、`armeabi-v7a` 和 `x86_64` 三套 Android ABI。

## Steam 账号与个人资料

- 修复社区页面“个人资料”“截图”“指南”“讨论”和“群组”等按钮点击没有反应的问题，
  这些公开 Steam 社区页面现在使用应用内浏览器打开。
- Steam 身份信息改为优先显示好友码，点击后可查看并复制 SteamID、SteamID3 和
  SteamID64，也可直接打开对应的 Steam 社区个人资料。
- 令牌账号详情、好友详情和其他用户个人资料统一复用同一套身份信息组件，不再只显示
  SteamID64。
- 账号切换面板中的现有账号与“添加 Steam 账号”入口统一使用相同尺寸、圆角和信息层级，
  添加入口不再像独立列表项。
- 好友详情中的入口改为“查看账号资料”，可进入完整的对方资料页查看游戏、成就及社区数据。
- 账号资料页新增徽章、好友和群组统计，并增加徽章横向预览。
- 新增完整徽章页面，可按“全部”“已解锁”“未解锁”筛选；徽章卡片支持查看等级、经验值、
  解锁状态和 Steam 官方详情。
- 徽章数据支持读取 Steam 分页内容，并识别尚未解锁或尚未合成的徽章。单项社区数据加载失败时，
  其余账号资料仍可正常显示。
- 大型徽章列表仅在当前会话保留完整数据，缓存保留前 250 枚及真实总数，减少极端账号造成的
  存储膨胀和界面卡顿。
- 账号资料页顶部支持跟随“Steam 动态个人资料背景”设置播放动画，并保留渐变遮罩保证资料文字清晰。
- 好友、群组和完美通关统计卡片支持进入对应列表；好友可继续进入对方资料，群组可查看成员统计及
  Steam 官方群组页面，完美通关列表可进入成就详情。
- 当前账号的完美通关统计会复用游戏库中的成就进度，避免资料接口暂时缺少进度时错误显示为 0。
- 修复 Steam 成就批量查询的 AppID 编码错误，个人资料与完美通关列表现在会识别整库成就进度，
  不再只显示极少数或首个全成就游戏。
- 资料信息中的好友码改为与地区、加入时间相同的行式布局，同时保留查看和复制全部 Steam ID 的功能。
- 未解锁徽章改为以徽章图案和锁定标记为主体，移除占据主要视觉的大块实色卡片。

## Steam 确认与通知

- 修复刷新 Steam 通知后，确认页面为空却仍显示确认红点的问题。通知中的待确认数量现在仅用于触发
  真实确认列表更新，菜单红点以当前账号成功读取的确认项目为准。
- 确认页面支持下拉刷新，并使用统一的带背景 Loading indicator 显示刷新状态。
- Steam 确认接口失败时会显示错误和重试入口，不再把会话失效等请求错误显示成“暂无待确认项目”。
- 切换账号或连续刷新时会忽略旧账号和旧请求返回的结果，避免确认数量及列表相互覆盖。

## 商店与游戏库

- 商店详情页侧边快捷工具栏缩小尺寸，闲置后自动收缩成贴边圆点；工具栏与圆点均可拖动，松手后会
  自动吸附到屏幕左侧或右侧，并保留拖动后的垂直位置。
- 游戏库列表移除右侧箭头，改为紧凑显示已完成成就数与完成比例；全成就游戏额外使用奖杯和主题色突出显示。
- 成就摘要改为独立的后台批量同步，不再阻塞游戏库、价格和库存刷新。每个账号首次使用时会按每批
  100 款游戏补全整库成就，完成状态会写入加密的游戏库缓存。
- 首次全量同步完成后，普通刷新只更新总游玩时长发生增长的游戏，未继续游玩的库存不会重复请求成就。
- 游戏库右上角菜单新增“同步全部成就”，可在数据遗漏或 Steam 返回异常后手动强制重新核对整库。
- 改善启用 Steam 家庭监护（Family View）账号的商店兼容性。原生商店遇到受限的 403 响应时，
  现在会提供 Steam 官方解锁入口；输入 PIN 返回 Monica Steam 后会自动刷新当前主页、筛选或游戏详情。
- Family View 解锁状态仅以 Steam 官方返回的 `steamparental` Cookie 在内存中按 SteamID 隔离保存，
  不保存、同步或自动填写 PIN，也不会把 Cookie 写入日志；重新创建内置网页时会恢复对应账号的短期会话。
- 修复应用内 Steam 商店网页顶部“浏览”“推荐”“更多”等菜单无法展开的问题。防吸顶适配不再遍历或
  改写 Steam 的 React 菜单节点，并为菜单保留真实触控区域；页面滚动时菜单仍会随网页正常离开。
- 商店游戏详情头图右侧改为竖向 M3E 图标工具栏，不再用大号“查看购买选项”文字按钮遮挡头图。
- 修复详情页竖向工具栏仍嵌在头图滚动区域的问题。工具栏现在固定悬浮在屏幕右侧，滚动页面时保持位置，
  并避开状态栏、系统导航区和不同样式 Dock，不再被价格卡片或滚动容器裁剪遮挡。
- 工具栏提供购买选项、Steam 官方商店、玩家评价和分享四个入口；购买与评价可直接定位到页面对应区域，
  缺少原生评价数据时会打开 Steam 官方评价位置。
- 游戏分享面板支持搜索并选择 Steam 好友，也可调用 Android 系统分享发送公开商店链接。
- 好友分享复用聊天页的 Outbox、会话恢复、发送核验和本地缓存链路，发送状态与普通聊天保持一致。
- 商店游戏详情页在平台信息下方显示 Steam 用户标签，默认紧凑展示并支持展开全部标签；点击标签可
  返回商店并应用对应的高级筛选。该区域复用“显示游戏标签”设置，关闭后详情页标签同步隐藏。
- 修复商店游戏详情中的“官方网站”按钮没有响应的问题。Steam 域名继续使用应用内浏览器，
  游戏厂商的外部官网交由系统浏览器安全打开，并兼容缺少协议前缀的网址。
- 多区价格改为更舒展的双列卡片，分别显示当地价格和人民币换算，不再把区服、价格、折扣
  与原价挤在同一行；点击卡片可展开或收起该区的 IsThereAnyDeal 全平台史低详情。
- 史低详情会核对所选区服币种和当前 Steam 价格。遇到地区回退、币种不符，或 ITAD 记录的
  “史低”高于当前 Steam 价格时会隐藏错误结果，避免误导。
- 史低功能使用用户自行申请的 IsThereAnyDeal API Key；密钥经过 Android Keystore 支持的
  加密存储，仅通过 `ITAD-API-Key` 请求头发送，不会写入请求地址或日志。
- 新增史低缓存、离线缓存回退和 `Retry-After` 限流处理。ITAD 暂时不可用时不会阻塞
  Steam 商店详情或多区价格加载。
- 展开的史低详情可打开 IsThereAnyDeal 官方游戏页面，并明确标注数据来源。Monica Steam 与
  IsThereAnyDeal 没有隶属或授权关系。
- 游戏库详情页的价格标题会根据当前 Steam 账号地区显示，不再出现港币价格配“国区价格”
  等错误组合。
- 删除拥有状态卡片中“已按照当前选择的 Steam 账号核对”的冗余说明；缓存、网络异常及
  家庭共享来源等有实际意义的状态仍会显示。
- 商店详情页将本体游戏、试玩版和 DLC 分为“相关游戏”“试玩版”“相关 DLC”，避免使用
  含义不清的“相关内容”。
- 官方网站改为独立的完整按钮，不再把“官方网站”和长 URL 挤在同一信息行中。
- 统一“加入购物车”Split Button 两侧高度，增大右侧展开按钮的触控与显示区域。
- “关于此游戏”默认折叠较长内容，可按需展开或收起。
- 全屏图片查看器新增双击放大与再次双击还原，并保留双指缩放、拖动、左右切换和下载。
- 玩家评价新增“查看玩家”入口，可使用当前应用内浏览器打开评价作者的 Steam 资料页。
- 玩家评价筛选改为一个紧凑入口，评价类型与时间范围集中在同一筛选面板中，当前筛选摘要会显示在入口右侧。
- 玩家评价正文支持常用 Markdown 与 Steam BBCode，可显示粗体、斜体、下划线、删除线、代码、引用、
  标题、列表和链接，不再直接露出格式标记。
- 删除购买操作区中的重复“查看购买选项”全宽按钮。

## Steam 聊天

- 聊天输入框左侧回形针改为无圆形底色的轻量图标，同时保留完整触控范围；点击后先在输入框下方展开
  附件面板，可分别选择“相册”中的图片/视频或受支持的文件，不再直接跳入系统文件管理器。
- 群聊遇到 Steam CM 短暂断线时不再弹出英文错误；已缓存或已恢复的消息会继续显示，后台实时连接与
  定时同步会自动重试。创建群组、邀请成员等操作若暂时无法连接，也会改为中文提示。
- 聊天可将单独发送的 Steam 商店游戏链接解析为原生游戏卡片，显示游戏头图、名称和查看商店入口。
- 支持“游戏名称 + 换行 + Steam 商店链接”以及独立的 Steam 商店 BBCode 链接；普通正文中夹带的
  商店链接和包含多个链接的消息仍按可点击文字显示，避免误判整条消息。
- 游戏分享通知会显示游戏名称或商店链接摘要。
- 支持 Steam 官方 `[url=地址]文字[/url]` 与 `[url]地址[/url]` BBCode 链接格式。
- BBCode 链接、普通网址和 Steam 表情可以在同一条消息中同时显示，链接文字不再被误解析为独立附件卡片。
- 普通聊天消息新增轻量 Markdown 与 Steam BBCode 渲染，并继续保留官方 Steam 表情、动态贴纸、
  游戏邀请和商店游戏卡片的原有显示方式。
- 支持 Steam 当前带 `src`、缩略图、响应式尺寸等属性的图片 BBCode；这类消息会直接显示高清图片，
  不再把完整 `[img ...]` 标签和长链接塞进文字气泡。
- 修复聊天图片附件上传初始化与云端上传协议：保留 Steam 签发的云存储授权头，统一 Cookie 与表单会话，
  登录会话失效时会强制刷新并仅重试一次。
- Steam 受限账户禁止上传图片时会显示明确提示，不再只显示模糊的 HTTP 400；其他 Steam 文件或服务错误
  也会按会话、文件限制和服务异常分类提示。
- 单独发送的图片改为无气泡背景、无边框的直接预览，时间与发送状态叠放在图片右下角；视频和压缩包
  继续使用专用附件卡片，普通链接使用内联样式并可点击打开。
- 聊天与评价中的富文本链接仅允许 `http`、`https` 和 `steam` 协议，未知协议只显示文字。

## 免责声明

Monica Steam 是非官方第三方客户端，与 Valve Corporation 或 Steam 没有隶属、授权或
合作关系。Steam、Steam Guard 及相关商标归 Valve Corporation 所有。
